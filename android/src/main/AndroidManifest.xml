<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Background continuity + notification permissions the plugin uses.
         The consuming app still declares these in its own manifest; these
         entries make the plugin self-describing and merge into the app. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application>
        <!-- Foreground service that keeps MCP connections alive while the app
             is backgrounded (BackgroundPolicy.keepAlive). -->
        <service
            android:name=".BackgroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
    </application>
</manifest>
